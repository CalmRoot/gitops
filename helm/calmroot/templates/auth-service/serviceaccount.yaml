apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.authService.serviceAccount.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: auth-service
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.authService.serviceAccount.roleArn | quote }}
