apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.therapistService.serviceAccount.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: therapist-service
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.therapistService.serviceAccount.roleArn | quote }}
